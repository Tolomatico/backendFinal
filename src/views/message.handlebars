<div class="mt-8 mx-auto max-w-md">
    <div class="bg-white space-y-10 py-8 px-4 shadow">
        <p class="text-center text-2xl font-extrabold text-center">{{pag}}</p>
        <p class="text-center text-xl font-extrabold text-center">{{message}}</p>
        {{#if email}}
        <p class="text-center text-xl font-extrabold text-center">{{email}}</p>
        {{/if}}
    </div>
</div>