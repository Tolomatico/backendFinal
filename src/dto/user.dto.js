class UserDTO {
    constructor(firstName, lastName, age, email, rol) {
        this.first_name = firstName,
            this.last_name = lastName,
            this.age = age,
            this.email = email,
            this.rol = rol
    }
}

module.exports = UserDTO